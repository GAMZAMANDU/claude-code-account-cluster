#!/usr/bin/env bash
set -euo pipefail

echo "Syncing credentials from Keychain..."
security find-generic-password -s "Claude Code-credentials" -w > ~/.claude/.credentials.json

echo "Saving account snapshot..."
cc-switch

echo "Logging out..."
claude auth logout

echo "Done. Run 'cc-switch <index>' to switch accounts."
